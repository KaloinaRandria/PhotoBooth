Choix de la base de donnée :
    -Utilisation de postgreSQL
    -Plus de fonctionnalité que MySql 
    -Plus de types de données sont supportés par PostgreSQL que mySQL
    -OpenSource donc moins cher que Oracle qui est un produit commercial
    -Peut effectuer des requêtes complexes 
    -tres flexible, facile à personnaliser

Utilisation
    -Cryptage du mot de passe : bcrypt
    -exemple de cryptage crypt('mon_mot_de_passe', gen_salt('bf'))
    -utilisation de l'extension pgcrypto (deja mis dans base.sql)
    -le cryptage ne sera pas necessaire si gerer du cote back
    -format du mail nom_adresse@(gmail/hotmail/yahoo).com --peut être modifier 
    -conseil: il est preferable de garder le formatage du mail pour plus de securiter(double securite base et back) ceci est juste une indication, pas obligé d'en prendre compte
    -aucune valeur null dans la table membre

indication 
    -Bien lire les commentaires de la base car il y a des propositions

id_format : fonction.sql --> fonction pour parametrer les id. Permet le formatage automatique des id lors de l'insertion de données
    salle : SAL1
    categorie : CAT1
    theme : THE1
    materiel : MAT1
    service : SERV1
    cleint : CLI1
    reservatin : RES1
    membre : MEMBRE1
